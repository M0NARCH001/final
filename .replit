modules = ["python-3.10"]

run = "cd backend && pip install -r requirements.txt && uvicorn app.api.main:app --host 0.0.0.0 --port 8080"

[nix]
channel = "stable-23_05"

[env]
PYTHONPATH = "$PYTHONPATH:$REPL_HOME/backend"
